version: '3.8'

services:
  web:
    build: .
    container_name: html-css-project
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    driver: bridge